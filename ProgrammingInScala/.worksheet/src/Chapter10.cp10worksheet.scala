package Chapter10

import Element.elem
import Spiral.spiral


object cp10worksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(121); val res$0 = 
    Array.fill(3)("|") mkString "\n";System.out.println("""res0: String = """ + $show(res$0));$skip(25); val res$1 = 
    Array.fill(1)("-"*3);System.out.println("""res1: Array[String] = """ + $show(res$1))}
  
  
                                    
                                    
  
}
